services:
  bbht:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - bbht-data:/data
    restart: unless-stopped

volumes:
  bbht-data:
